package lambda;
// Functional interface to perform sum operation

/**
 * Functional interface to perform a sum operation on two integers.
 */
@FunctionalInterface
public interface SecondInterfaceSum {
    Integer sum(Integer a,Integer b);
}

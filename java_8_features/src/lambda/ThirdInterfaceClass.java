package lambda;
// Functional interface to calculate the length of a string

/**
 * Functional interface to calculate the length of a given string.
 */
@FunctionalInterface
public interface ThirdInterfaceClass {

     int getLength(String str);

}
